import HomeDescription from "../Home/HomeDescription";

const HeroImage = () => {
  return (
    <div
      style={{
        background: "#fff",
      }}
      id="hero"
    >
      <HomeDescription />
    </div>
  );
};

export default HeroImage;
